name: TANI V14.0 ANEH AHENG QUANTUM 🔬👽💀☠️
on:
  schedule:
    - cron: '0,30 * * * *'
  workflow_dispatch:

concurrency:
  group: tani-v14-quantum
  cancel-in-progress: false

jobs:
  tani-quantum:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Init files quantum
        run: |
          touch .dna_tani_v12.json .memory_tani_v12.json .last_tani_v12.json .offset_history.json .quantum_v14.json || true
          ls -lh .*.json || echo "init"

      - name: Cache Quantum DNA
        uses: actions/cache@v4
        with:
          path: |
            .dna_tani_v12.json
            .memory_tani_v12.json
            .last_tani_v12.json
            .offset_history.json
            .quantum_v14.json
          key: tani-v14-${{ hashFiles('v14.py') }}
          restore-keys: |
            tani-v14-
            tani-v12-garang-perfect-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests pandas yfinance pytz numpy --quiet --no-cache-dir

      - name: Run V14
        run: python v14.py
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

      - name: Show Gudang
        if: always()
        run: |
          echo "=== GUDANG V14 QUANTUM ==="
          cat .memory_tani_v12.json || echo "init"
          cat .quantum_v14.json || echo "no quantum"
          ls -lh .*.json
